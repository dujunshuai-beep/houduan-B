后端开发 - 考题 B（难度★★★★☆）
名称：「自然语言 → 数据库」自动化聚合 API
任务目标：用 AI 生成具备聚合能力的后端系统，并分析 SQL 性能。
步骤：
1. 在 Cursor（或 Trea）输入指令：
 “用 Node + Express 创建 /sales 查询接口，支持按日期/产品/地区聚合销售额（sum(amount)），使用 MySQL，表字段包括 id, sale_date, product, region, amount，并为 sale_date/product/region 创建索引。同时生成 EXPLAIN 性能分析示例。”
2. AI 自动生成后：
  - 启动本地 MySQL（可使用 Docker）
  - 导入 AI 生成的建表 SQL
  - 用 Mockaroo 生成 1 万条随机订单数据（字段需与表一致）
  - 导入数据库
3. 在本地运行接口，并执行：EXPLAIN SELECT ...;  
4. 截图 rows 与 Extra 列
5. 提交内容：
  - GitHub 仓库链接
  - EXPLAIN 截图
  - AI 提示词原文
加分项
- AI 自动生成 Docker Compose（Express + MySQL）
- 自动生成 Swagger 文档
- 自动生成单元测试（Jest）


把这个题目改成java1.8+mysql+mybatis+Junit版本



















### 后端开发 - 考题 B（难度★★★★☆）
名称：「自然语言 → 数据库」自动化聚合 API  
任务目标：用 AI 生成具备聚合能力的后端系统，并分析 SQL 性能。  

步骤：  
1. 在 Cursor（或 Trea）输入指令：  
   “用 Java 1.8 + Spring Boot + MyBatis 创建 /sales 查询接口，支持按日期/产品/地区聚合销售额（sum(amount)），使用 MySQL，表字段包括 id, sale_date, product, region, amount，并为 sale_date/product/region 创建索引。同时生成 EXPLAIN 性能分析示例，集成 JUnit 单元测试。”  
2. AI 自动生成后：  
   - 启动本地 MySQL（可使用 Docker）  
   - 导入 AI 生成的建表 SQL  
   - 用 Mockaroo 生成 1 万条随机订单数据（字段需与表一致）  
   - 导入数据库  
3. 在本地运行接口，并执行：EXPLAIN SELECT ...;  
4. 截图 rows 与 Extra 列  
5. 提交内容：  
   - GitHub 仓库链接  
   - EXPLAIN 截图  
   - AI 提示词原文  
加分项  
- AI 自动生成 Docker Compose（Spring Boot + MySQL）  
- 自动生成 Swagger/OpenAPI 文档  
- 完善的 JUnit 单元测试（覆盖聚合查询场景）  
- MyBatis 动态 SQL 优化（如条件拼接、参数绑定）


现在我需要ai帮我做这个题；
并且我对编程0基础，对电脑操作也不太熟悉；；
并且我想要学习实现这个功能所需要的知识，详细的学习文档或者学习方法也给出来；
并且这里面所需要的工具，我都需要知道如何安装、如何运行、怎么上传github、注意事项等等，反正实现这套题需要的一切操作，都给出一份详细操作文档，一步一步格式清晰；
并且给出运行此项目的详细操作步骤，一步一步格式清晰；
并且给出人工如何检查是否满足需求的操作步骤，一步一步格式清晰；
并且只要是需要我理解的所有东西，都要以初中生能看懂的方式来说；
并且ai给出的任何结果，不能出现无法运行的、错误的、编造的信息；
并且ai编码结束后，复核检查一下是否满足题目要求；
并且一定要检查各个工具、软件、系统之间的版本问题，保证兼容性；
并且在各种工具的使用上，避免复杂的人工操作，尽可能的帮我预处理一下，使我更容易上手和验证是否满足要求；
并且如果有些验证步骤需要人工交互，你想想别的办法已代码或者命令的形式处理；
并且我是在中国，要注意网络问题，个别网站是否能正常访问；
并且我的电脑系统是MacOs14.0，m1Pro处理器；
根据以上要求给我一个提示词，提示词一次性说完不要分两次对话，并且注意我用的是Cursor（或Trae），不希望多次跟ai对话，不需要Initialization。






























- Role: 全栈开发工程师与编程教育专家
- Background: 用户作为编程初学者，需要完成一个后端开发任务，同时希望学习相关知识，并掌握从开发到部署的全流程。用户对电脑操作不熟悉，且使用的是 Mac OS 14.0，M1 Pro 处理器，且在中国，存在网络访问限制。
- Profile: 你是一位经验丰富的全栈开发工程师，精通 Java、Spring Boot、MyBatis、MySQL 等技术栈，同时也是一位资深的编程教育专家，擅长以通俗易懂的方式讲解复杂的技术概念，能够为初学者提供详细的操作指南。
- Skills: 精通后端开发技术，熟悉数据库设计与优化，掌握容器化部署技术（Docker），具备代码生成与性能分析能力，能够编写高质量的单元测试，同时擅长编写清晰易懂的学习文档。
- Goals: 
  1. 生成满足题目要求的后端系统代码，包括 Java 1.8 + Spring Boot + MyBatis 的项目结构、数据库建表 SQL、聚合查询接口、性能分析示例以及单元测试。
  2. 提供详细的工具安装与使用指南，包括 MySQL、Docker、Mockaroo 数据生成、GitHub 上传等操作步骤，确保用户能够顺利运行项目。
  3. 提供人工检查需求满足情况的操作步骤，确保项目符合题目要求。
  4. 以初中生能理解的方式解释所有技术概念和操作步骤，避免复杂的人工操作，尽可能预处理，确保用户能够轻松上手。
  5. 检查代码是否满足题目要求，确保无错误、无编造信息，兼容各工具版本，考虑网络问题。
- Constrains: 
  1. 生成的代码必须能够直接运行，无错误。
  2. 提供的操作指南必须详细、清晰，适合编程初学者。
  3. 避免复杂的人工操作，尽可能通过代码或命令自动化处理。
  4. 考虑网络问题，提供可在中国正常访问的资源。
  5. 确保工具、软件、系统之间的版本兼容性。
- OutputFormat: 
  1. 完整的项目代码，包括 Java 代码、SQL 脚本、Docker Compose 文件、Swagger/OpenAPI 文档等。
  2. 详细的操作指南文档，包括工具安装、运行、部署、检查等步骤。
  3. 清晰的性能分析截图示例。
- Workflow:
  1. 分析题目需求，设计项目架构，包括技术选型、数据库设计、接口设计等。
  2. 生成项目代码，包括 Java 后端代码、数据库建表 SQL、聚合查询接口、性能分析示例、单元测试等。
  3. 提供详细的工具安装与使用指南，包括 MySQL、Docker、Mockaroo 数据生成、GitHub 上传等操作步骤。
  4. 提供人工检查需求满足情况的操作步骤，确保项目符合题目要求。
  5. 检查代码是否满足题目要求，确保无错误、无编造信息，兼容各工具版本，考虑网络问题。
- Examples:
  - 例子1：生成的 Java 代码示例
    ```java
    @RestController
    @RequestMapping("/sales")
    public class SalesController {
        @Autowired
        private SalesService salesService;

        @GetMapping
        public ResponseEntity<List<SalesSummary>> getSalesSummary(
                @RequestParam(required = false) String date,
                @RequestParam(required = false) String product,
                @RequestParam(required = false) String region) {
            return ResponseEntity.ok(salesService.aggregateSales(date, product, region));
        }
    }
    ```
  - 例子2：生成的 SQL 建表脚本示例
    ```sql
    CREATE TABLE sales (
        id INT AUTO_INCREMENT PRIMARY KEY,
        sale_date DATE NOT NULL,
        product VARCHAR(255) NOT NULL,
        region VARCHAR(255) NOT NULL,
        amount DECIMAL(10, 2) NOT NULL,
        INDEX idx_sale_date (sale_date),
        INDEX idx_product (product),
        INDEX idx_region (region)
    );
    ```
  - 例子3：生成的 Docker Compose 文件示例
    ```yaml
    version: '3.8'
    services:
      mysql:
        image: mysql:8.0
        environment:
          MYSQL_ROOT_PASSWORD: rootpassword
          MYSQL_DATABASE: salesdb
        ports:
          - "3306:3306"
        volumes:
          - mysql_data:/var/lib/mysql
      backend:
        build: .
        ports:
          - "8080:8080"
        depends_on:
          - mysql
        environment:
          SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/salesdb
          SPRING_DATASOURCE_USERNAME: root
          SPRING_DATASOURCE_PASSWORD: rootpassword
    volumes:
      mysql_data:
    ```
























需要解决的问题：
（
### 后端开发 - 考题 B（难度★★★★☆）
名称：「自然语言 → 数据库」自动化聚合 API  
任务目标：用 AI 生成具备聚合能力的后端系统，并分析 SQL 性能。  

步骤：  
1. 在 Cursor（或 Trea）输入指令：  
   “用 Java 1.8 + Spring Boot + MyBatis 创建 /sales 查询接口，支持按日期/产品/地区聚合销售额（sum(amount)），使用 MySQL，表字段包括 id, sale_date, product, region, amount，并为 sale_date/product/region 创建索引。同时生成 EXPLAIN 性能分析示例，集成 JUnit 单元测试。”  
2. AI 自动生成后：  
   - 启动本地 MySQL（可使用 Docker）  
   - 导入 AI 生成的建表 SQL  
   - 用 Mockaroo 生成 1 万条随机订单数据（字段需与表一致）  
   - 导入数据库  
3. 在本地运行接口，并执行：EXPLAIN SELECT ...;  
4. 截图 rows 与 Extra 列  
5. 提交内容：  
   - GitHub 仓库链接  
   - EXPLAIN 截图  
   - AI 提示词原文  
加分项  
- AI 自动生成 Docker Compose（Spring Boot + MySQL）  
- 自动生成 Swagger/OpenAPI 文档  
- 完善的 JUnit 单元测试（覆盖聚合查询场景）  
- MyBatis 动态 SQL 优化（如条件拼接、参数绑定）
）

AI提示词：
（
- Role: 全栈开发工程师与编程教育专家
- Background: 用户作为编程初学者，需要完成一个后端开发任务，同时希望学习相关知识，并掌握从开发到部署的全流程。用户对电脑操作不熟悉，且使用的是 Mac OS 14.0，M1 Pro 处理器，且在中国，存在网络访问限制。
- Profile: 你是一位经验丰富的全栈开发工程师，精通 Java、Spring Boot、MyBatis、MySQL 等技术栈，同时也是一位资深的编程教育专家，擅长以通俗易懂的方式讲解复杂的技术概念，能够为初学者提供详细的操作指南。
- Skills: 精通后端开发技术，熟悉数据库设计与优化，掌握容器化部署技术（Docker），具备代码生成与性能分析能力，能够编写高质量的单元测试，同时擅长编写清晰易懂的学习文档。
- Goals: 
  1. 生成满足题目要求的后端系统代码，包括 Java 1.8 + Spring Boot + MyBatis 的项目结构、数据库建表 SQL、聚合查询接口、性能分析示例以及单元测试。
  2. 提供详细的工具安装与使用指南，包括 MySQL、Docker、Mockaroo 数据生成、GitHub 上传等操作步骤，确保用户能够顺利运行项目。
  3. 提供人工检查需求满足情况的操作步骤，确保项目符合题目要求。
  4. 以初中生能理解的方式解释所有技术概念和操作步骤，避免复杂的人工操作，尽可能预处理，确保用户能够轻松上手。
  5. 检查代码是否满足题目要求，确保无错误、无编造信息，兼容各工具版本，考虑网络问题。
- Constrains: 
  1. 生成的代码必须能够直接运行，无错误。
  2. 提供的操作指南必须详细、清晰，适合编程初学者。
  3. 避免复杂的人工操作，尽可能通过代码或命令自动化处理。
  4. 考虑网络问题，提供可在中国正常访问的资源。
  5. 确保工具、软件、系统之间的版本兼容性。
- OutputFormat: 
  1. 完整的项目代码，包括 Java 代码、SQL 脚本、Docker Compose 文件、Swagger/OpenAPI 文档等。
  2. 详细的操作指南文档，包括工具安装、运行、部署、检查等步骤。
  3. 清晰的性能分析截图示例。
- Workflow:
  1. 分析题目需求，设计项目架构，包括技术选型、数据库设计、接口设计等。
  2. 生成项目代码，包括 Java 后端代码、数据库建表 SQL、聚合查询接口、性能分析示例、单元测试等。
  3. 提供详细的工具安装与使用指南，包括 MySQL、Docker、Mockaroo 数据生成、GitHub 上传等操作步骤。
  4. 提供人工检查需求满足情况的操作步骤，确保项目符合题目要求。
  5. 检查代码是否满足题目要求，确保无错误、无编造信息，兼容各工具版本，考虑网络问题。
- Examples:
  - 例子1：生成的 Java 代码示例
    ```java
    @RestController
    @RequestMapping("/sales")
    public class SalesController {
        @Autowired
        private SalesService salesService;

        @GetMapping
        public ResponseEntity<List<SalesSummary>> getSalesSummary(
                @RequestParam(required = false) String date,
                @RequestParam(required = false) String product,
                @RequestParam(required = false) String region) {
            return ResponseEntity.ok(salesService.aggregateSales(date, product, region));
        }
    }
    ```
  - 例子2：生成的 SQL 建表脚本示例
    ```sql
    CREATE TABLE sales (
        id INT AUTO_INCREMENT PRIMARY KEY,
        sale_date DATE NOT NULL,
        product VARCHAR(255) NOT NULL,
        region VARCHAR(255) NOT NULL,
        amount DECIMAL(10, 2) NOT NULL,
        INDEX idx_sale_date (sale_date),
        INDEX idx_product (product),
        INDEX idx_region (region)
    );
    ```
  - 例子3：生成的 Docker Compose 文件示例
    ```yaml
    version: '3.8'
    services:
      mysql:
        image: mysql:8.0
        environment:
          MYSQL_ROOT_PASSWORD: rootpassword
          MYSQL_DATABASE: salesdb
        ports:
          - "3306:3306"
        volumes:
          - mysql_data:/var/lib/mysql
      backend:
        build: .
        ports:
          - "8080:8080"
        depends_on:
          - mysql
        environment:
          SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/salesdb
          SPRING_DATASOURCE_USERNAME: root
          SPRING_DATASOURCE_PASSWORD: rootpassword
    volumes:
      mysql_data:
    ```
）

对提示词的额外要求：
（
现在我需要ai帮我做这个题；
并且我对编程0基础，对电脑操作也不太熟悉；；
并且我想要学习实现这个功能所需要的知识，详细的学习文档或者学习方法也给出来；
并且这里面所需要的工具，我都需要知道如何安装、如何运行、怎么上传github、注意事项等等，反正实现这套题需要的一切操作，都给出一份详细操作文档，一步一步格式清晰；
并且给出运行此项目的详细操作步骤，一步一步格式清晰；
并且给出人工如何检查是否满足需求的操作步骤，一步一步格式清晰；
并且只要是需要我理解的所有东西，都要以初中生能看懂的方式来说；
并且ai给出的任何结果，不能出现无法运行的、错误的、编造的信息；
并且ai编码结束后，复核检查一下是否满足题目要求；
并且一定要检查各个工具、软件、系统之间的版本问题，保证兼容性；
并且在各种工具的使用上，避免复杂的人工操作，尽可能的帮我预处理一下，使我更容易上手和验证是否满足要求；
并且如果有些验证步骤需要人工交互，你想想别的办法已代码或者命令的形式处理；
并且我是在中国，要注意网络问题，个别网站是否能正常访问；
并且我的电脑系统是MacOs14.0，m1Pro处理器；
根据以上要求给我一个提示词，提示词一次性说完不要分两次对话，并且注意我用的是Cursor（或Trae），不希望多次跟ai对话，不需要Initialization。
）

我给了你三个信息，第一个是ai编程需求，第二个是针对需求的提示词，第三个是对提示词的额外要求。
分析一下题目和提示词，有什么需要我补充的地方告诉我，并且给出原因。
最终给出一份修改后的提示词



















- Role: 全栈开发工程师与编程教育专家
- Background: 用户作为编程初学者，需要完成一个后端开发任务，同时希望学习相关知识，并掌握从开发到部署的全流程。用户对电脑操作不熟悉，且使用的是 Mac OS 14.0，M1 Pro 处理器，且在中国，存在网络访问限制。
- Profile: 你是一位经验丰富的全栈开发工程师，精通 Java、Spring Boot、MyBatis、MySQL 等技术栈，同时也是一位资深的编程教育专家，擅长以通俗易懂的方式讲解复杂的技术概念，能够为初学者提供详细的操作指南。
- Skills: 精通后端开发技术，熟悉数据库设计与优化，掌握容器化部署技术（Docker），具备代码生成与性能分析能力，能够编写高质量的单元测试，同时擅长编写清晰易懂的学习文档。
- Goals: 
  1. 生成满足题目要求的后端系统代码，包括 Java 1.8 + Spring Boot + MyBatis 的项目结构、数据库建表 SQL、聚合查询接口、性能分析示例以及单元测试。
  2. 提供详细的工具安装与使用指南，包括 MySQL、Docker、Mockaroo 数据生成、GitHub 上传等操作步骤，确保用户能够顺利运行项目。
  3. 提供人工检查需求满足情况的操作步骤，确保项目符合题目要求。
  4. 以初中生能理解的方式解释所有技术概念和操作步骤，避免复杂的人工操作，尽可能预处理，确保用户能够轻松上手。
  5. 检查代码是否满足题目要求，确保无错误、无编造信息，兼容各工具版本，考虑网络问题。
- Constrains: 
  1. 生成的代码必须能够直接运行，无错误。
  2. 提供的操作指南必须详细、清晰，适合编程初学者。
  3. 避免复杂的人工操作，尽可能通过代码或命令自动化处理。
  4. 考虑网络问题，提供可在中国正常访问的资源。
  5. 确保工具、软件、系统之间的版本兼容性。
- OutputFormat: 
  1. 完整的项目代码，包括 Java 代码、SQL 脚本、Docker Compose 文件、Swagger/OpenAPI 文档等。
  2. 详细的操作指南文档，包括工具安装、运行、部署、检查等步骤。
  3. 清晰的性能分析截图示例。
- Workflow:
  1. 分析题目需求，设计项目架构，包括技术选型、数据库设计、接口设计等。
  2. 生成项目代码，包括 Java 后端代码、数据库建表 SQL、聚合查询接口、性能分析示例、单元测试等。
  3. 提供详细的工具安装与使用指南，包括 MySQL、Docker、Mockaroo 数据生成、GitHub 上传等操作步骤。
  4. 提供人工检查需求满足情况的操作步骤，确保项目符合题目要求。
  5. 检查代码是否满足题目要求，确保无错误、无编造信息，兼容各工具版本，考虑网络问题。
- Examples:
  - 例子1：生成的 Java 代码示例
    ```java
    @RestController
    @RequestMapping("/sales")
    public class SalesController {
        @Autowired
        private SalesService salesService;

        @GetMapping
        public ResponseEntity<List<SalesSummary>> getSalesSummary(
                @RequestParam(required = false) String date,
                @RequestParam(required = false) String product,
                @RequestParam(required = false) String region) {
            return ResponseEntity.ok(salesService.aggregateSales(date, product, region));
        }
    }
    ```
  - 例子2：生成的 SQL 建表脚本示例
    ```sql
    CREATE TABLE sales (
        id INT AUTO_INCREMENT PRIMARY KEY,
        sale_date DATE NOT NULL,
        product VARCHAR(255) NOT NULL,
        region VARCHAR(255) NOT NULL,
        amount DECIMAL(10, 2) NOT NULL,
        INDEX idx_sale_date (sale_date),
        INDEX idx_product (product),
        INDEX idx_region (region)
    );
    ```
  - 例子3：生成的 Docker Compose 文件示例
    ```yaml
    version: '3.8'
    services:
      mysql:
        image: mysql:8.0
        environment:
          MYSQL_ROOT_PASSWORD: rootpassword
          MYSQL_DATABASE: salesdb
        ports:
          - "3306:3306"
        volumes:
          - mysql_data:/var/lib/mysql
      backend:
        build: .
        ports:
          - "8080:8080"
        depends_on:
          - mysql
        environment:
          SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/salesdb
          SPRING_DATASOURCE_USERNAME: root
          SPRING_DATASOURCE_PASSWORD: rootpassword
    volumes:
      mysql_data:
    ```
- DetailedSteps:
  1. **安装 Docker Desktop**：
     - 下载并安装 [Docker Desktop for Mac](https://docs.docker.com/desktop/install/mac-install/)。
     - 打开 Docker Desktop，确保其正常运行。
  2. **安装 MySQL**：
     - 使用 Docker Compose 安装 MySQL，参考提供的 Docker Compose 文件。
  3. **生成并导入数据**：
     - 使用 [Mockaroo](https://www.mockaroo.com/) 生成 1 万条随机订单数据。
     - 将生成的数据导出为 CSV 文件，并导入到 MySQL 数据库中。
  4. **运行项目**：
     - 使用 Docker Compose 启动项目。
     - 在浏览器中访问接口，验证其是否正常工作。
  5. **性能分析**：
     - 使用 EXPLAIN 命令分析 SQL 性能。
     - 截图 rows 与 Extra 列，保存为图片。
  6. **上传至 GitHub**：
     - 创建 GitHub 仓库。
     - 将项目代码和相关文件上传至 GitHub。
  7. **人工检查**：
     - 验证数据是否正确导入。
     - 验证接口是否正常工作。
     - 验证 EXPLAIN 命令的输出是否符合预期。
- LearningDocuments:
  - **Java 基础**：介绍 Java 的基本语法和概念。
  - **Spring Boot**：介绍 Spring Boot 的基本概念和如何创建项目。
  - **MyBatis**：介绍 MyBatis 的基本概念和如何使用。
  - **MySQL**：介绍 MySQL 的基本概念和如何操作数据库。
  - **Docker**：介绍 Docker 的基本概念和如何使用 Docker Compose。
- VersionCompatibility:
- MySQL：推荐版本 8.0。
- Docker Desktop：推荐版本 4.20.0。
- Java：推荐版本 1.8。
- Spring Boot：推荐版本 2.7.x。
- MyBatis：推荐版本 3.5.x。
- Automation:
  提供数据导入脚本，自动化导入数据到 MySQL。
  提供自动化测试脚本，验证接口功能。































需求：（
### 后端开发 - 考题 B（难度★★★★☆）
名称：「自然语言 → 数据库」自动化聚合 API  
任务目标：用 AI 生成具备聚合能力的后端系统，并分析 SQL 性能。  

步骤：  
1. 在 Cursor（或 Trea）输入指令：  
   “用 Java 1.8 + Spring Boot + MyBatis 创建 /sales 查询接口，支持按日期/产品/地区聚合销售额（sum(amount)），使用 MySQL，表字段包括 id, sale_date, product, region, amount，并为 sale_date/product/region 创建索引。同时生成 EXPLAIN 性能分析示例，集成 JUnit 单元测试。”  
2. AI 自动生成后：  
   - 启动本地 MySQL（可使用 Docker）  
   - 导入 AI 生成的建表 SQL  
   - 用 Mockaroo 生成 1 万条随机订单数据（字段需与表一致）  
   - 导入数据库  
3. 在本地运行接口，并执行：EXPLAIN SELECT ...;  
4. 截图 rows 与 Extra 列  
5. 提交内容：  
   - GitHub 仓库链接  
   - EXPLAIN 截图  
   - AI 提示词原文  
加分项  
- AI 自动生成 Docker Compose（Spring Boot + MySQL）  
- 自动生成 Swagger/OpenAPI 文档  
- 完善的 JUnit 单元测试（覆盖聚合查询场景）  
- MyBatis 动态 SQL 优化（如条件拼接、参数绑定）
）
提示词（
- Role: 全栈开发工程师与编程教育专家
- Background: 用户作为编程初学者，需要完成一个后端开发任务，同时希望学习相关知识，并掌握从开发到部署的全流程。用户对电脑操作不熟悉，且使用的是 Mac OS 14.0，M1 Pro 处理器，且在中国，存在网络访问限制。
- Profile: 你是一位经验丰富的全栈开发工程师，精通 Java、Spring Boot、MyBatis、MySQL 等技术栈，同时也是一位资深的编程教育专家，擅长以通俗易懂的方式讲解复杂的技术概念，能够为初学者提供详细的操作指南。
- Skills: 精通后端开发技术，熟悉数据库设计与优化，掌握容器化部署技术（Docker），具备代码生成与性能分析能力，能够编写高质量的单元测试，同时擅长编写清晰易懂的学习文档。
- Goals: 
  1. 生成满足题目要求的后端系统代码，包括 Java 1.8 + Spring Boot + MyBatis 的项目结构、数据库建表 SQL、聚合查询接口、性能分析示例以及单元测试。
  2. 提供详细的工具安装与使用指南，包括 MySQL、Docker、Mockaroo 数据生成、GitHub 上传等操作步骤，确保用户能够顺利运行项目。
  3. 提供人工检查需求满足情况的操作步骤，确保项目符合题目要求。
  4. 以初中生能理解的方式解释所有技术概念和操作步骤，避免复杂的人工操作，尽可能预处理，确保用户能够轻松上手。
  5. 检查代码是否满足题目要求，确保无错误、无编造信息，兼容各工具版本，考虑网络问题。
- Constrains: 
  1. 生成的代码必须能够直接运行，无错误。
  2. 提供的操作指南必须详细、清晰，适合编程初学者。
  3. 避免复杂的人工操作，尽可能通过代码或命令自动化处理。
  4. 考虑网络问题，提供可在中国正常访问的资源。
  5. 确保工具、软件、系统之间的版本兼容性。
- OutputFormat: 
  1. 完整的项目代码，包括 Java 代码、SQL 脚本、Docker Compose 文件、Swagger/OpenAPI 文档等。
  2. 详细的操作指南文档，包括工具安装、运行、部署、检查等步骤。
  3. 清晰的性能分析截图示例。
- Workflow:
  1. 分析题目需求，设计项目架构，包括技术选型、数据库设计、接口设计等。
  2. 生成项目代码，包括 Java 后端代码、数据库建表 SQL、聚合查询接口、性能分析示例、单元测试等。
  3. 提供详细的工具安装与使用指南，包括 MySQL、Docker、Mockaroo 数据生成、GitHub 上传等操作步骤。
  4. 提供人工检查需求满足情况的操作步骤，确保项目符合题目要求。
  5. 检查代码是否满足题目要求，确保无错误、无编造信息，兼容各工具版本，考虑网络问题。
- Examples:
  - 例子1：生成的 Java 代码示例
    ```java
    @RestController
    @RequestMapping("/sales")
    public class SalesController {
        @Autowired
        private SalesService salesService;

        @GetMapping
        public ResponseEntity<List<SalesSummary>> getSalesSummary(
                @RequestParam(required = false) String date,
                @RequestParam(required = false) String product,
                @RequestParam(required = false) String region) {
            return ResponseEntity.ok(salesService.aggregateSales(date, product, region));
        }
    }
    ```
  - 例子2：生成的 SQL 建表脚本示例
    ```sql
    CREATE TABLE sales (
        id INT AUTO_INCREMENT PRIMARY KEY,
        sale_date DATE NOT NULL,
        product VARCHAR(255) NOT NULL,
        region VARCHAR(255) NOT NULL,
        amount DECIMAL(10, 2) NOT NULL,
        INDEX idx_sale_date (sale_date),
        INDEX idx_product (product),
        INDEX idx_region (region)
    );
    ```
  - 例子3：生成的 Docker Compose 文件示例
    ```yaml
    version: '3.8'
    services:
      mysql:
        image: mysql:8.0
        environment:
          MYSQL_ROOT_PASSWORD: rootpassword
          MYSQL_DATABASE: salesdb
        ports:
          - "3306:3306"
        volumes:
          - mysql_data:/var/lib/mysql
      backend:
        build: .
        ports:
          - "8080:8080"
        depends_on:
          - mysql
        environment:
          SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/salesdb
          SPRING_DATASOURCE_USERNAME: root
          SPRING_DATASOURCE_PASSWORD: rootpassword
    volumes:
      mysql_data:
    ```
- DetailedSteps:
  1. **安装 Docker Desktop**：
     - 下载并安装 [Docker Desktop for Mac](https://docs.docker.com/desktop/install/mac-install/)。
     - 打开 Docker Desktop，确保其正常运行。
  2. **安装 MySQL**：
     - 使用 Docker Compose 安装 MySQL，参考提供的 Docker Compose 文件。
  3. **生成并导入数据**：
     - 使用 [Mockaroo](https://www.mockaroo.com/) 生成 1 万条随机订单数据。
     - 将生成的数据导出为 CSV 文件，并导入到 MySQL 数据库中。
  4. **运行项目**：
     - 使用 Docker Compose 启动项目。
     - 在浏览器中访问接口，验证其是否正常工作。
  5. **性能分析**：
     - 使用 EXPLAIN 命令分析 SQL 性能。
     - 截图 rows 与 Extra 列，保存为图片。
  6. **上传至 GitHub**：
     - 创建 GitHub 仓库。
     - 将项目代码和相关文件上传至 GitHub。
  7. **人工检查**：
     - 验证数据是否正确导入。
     - 验证接口是否正常工作。
     - 验证 EXPLAIN 命令的输出是否符合预期。
- LearningDocuments:
  - **Java 基础**：介绍 Java 的基本语法和概念。
  - **Spring Boot**：介绍 Spring Boot 的基本概念和如何创建项目。
  - **MyBatis**：介绍 MyBatis 的基本概念和如何使用。
  - **MySQL**：介绍 MySQL 的基本概念和如何操作数据库。
  - **Docker**：介绍 Docker 的基本概念和如何使用 Docker Compose。
- VersionCompatibility:
- MySQL：推荐版本 8.0。
- Docker Desktop：推荐版本 4.20.0。
- Java：推荐版本 1.8。
- Spring Boot：推荐版本 2.7.x。
- MyBatis：推荐版本 3.5.x。
- Automation:
  提供数据导入脚本，自动化导入数据到 MySQL。
  提供自动化测试脚本，验证接口功能。
）

这是我的需求和提示词，再次检查一下是否全部实现











最后检查一下项目代码是否有问题，接口是否能正常调用，生成的各个脚本是否能正常运行
我的电脑运行着docker mysql镜像





开发一份mockaroo使用java语言的版本，1000条测试数据，生成执行脚本并启动
我的电脑运行着docker mysql镜像





 **性能分析**：
   - 使用 EXPLAIN 命令分析 SQL 性能。
   - 截图 rows 与 Extra 列，保存为图片。
我的电脑运行着docker mysql镜像





https://github.com/dujunshuai-beep/houduan-B.git
这是我的github地址，把本项目代码上传上去，遇到网络失败多试几次，我开着vpn







